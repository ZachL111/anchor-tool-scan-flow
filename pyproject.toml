[project]
name = "anchor-tool-scan-flow"
version = "0.1.0"
description = "Package a Python local lab for scan analysis with deny and allow fixtures, explainable decision traces, and documented operating limits."
requires-python = ">=3.11"
